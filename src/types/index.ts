export interface NavItem {
  label: string;
  href: string;
}

export interface HeroProps {
  title: string;
  subtitle: string;
  ctaText: string;
  ctaLink: string;
}

export interface CardProps {
  imageUrl: string;
  artist: string;
  title: string;
}